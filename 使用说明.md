# 供应链智能仓储管理系统 (WMS) — 使用说明

> 版本：v1.0 | 更新：2026-06-15 | 课程：MIS 系统课程实践

---

## 目录

1. [系统概述](#1-系统概述)
2. [环境要求与部署](#2-环境要求与部署)
3. [启动系统](#3-启动系统)
4. [用户角色与登录](#4-用户角色与登录)
5. [功能模块使用指南](#5-功能模块使用指南)
6. [REST API 文档](#6-rest-api-文档)
7. [数据库说明](#7-数据库说明)
8. [常见问题](#8-常见问题)

---

## 1. 系统概述

WMS（Warehouse Management System）是一套供应链智能仓储管理系统，实现采购入库全流程数字化管理。

### 1.1 技术栈

| 层次 | 技术 |
|------|------|
| **前端** | Vue 3 + Vite + Pinia + Vue Router + Element Plus + Axios |
| **后端** | C++17 + cpp-httplib + JWT (HMAC-SHA256 via Windows CNG) |
| **数据库** | Oracle 23ai Free (可选，支持内存降级模式) |
| **构建** | CMake + MinGW GCC 13.2.0 (后端) / Vite (前端) |

### 1.2 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    浏览器 (Vue 3)                        │
│  Login · Dashboard · Inbound · SKU · Supplier · Settings │
└────────────────────────┬────────────────────────────────┘
                         │ REST API (JSON + JWT)
                         ▼
┌─────────────────────────────────────────────────────────┐
│              C++17 后端 (cpp-httplib)                     │
│  AuthController · InventoryController                     │
│  SkuController · SupplierController                       │
│  InventoryService · Jwt (HMAC-SHA256)                     │
└────────────────────────┬────────────────────────────────┘
                         │ OCI / Direct SQL
                         ▼
┌─────────────────────────────────────────────────────────┐
│              Oracle 23ai Free / 内存存储                  │
│  tables · sequences · procedures · triggers               │
└─────────────────────────────────────────────────────────┘
```

### 1.3 核心业务流程

```
创建入库单 (DRAFT) → 提交 (SUBMITTED) → 收货确认 (RECEIVED)
                      ↓                       ↓
                   取消 (CANCELLED)     部分收货 (PARTIAL) → 继续收货
```

---

## 2. 环境要求与部署

### 2.1 后端依赖

| 依赖 | 版本 | 位置 |
|------|------|------|
| MinGW GCC | 13.2.0+ | `C:/MinGW/bin/g++.exe` |
| CMake | 3.20+ | — |
| cpp-httplib | 头文件版 | `D:/dev/db/httplib.h` |
| nlohmann/json | 头文件版 | `backend/include/nlohmann/json.hpp`（已捆绑） |
| Oracle Instant Client | 23ai Free (可选) | `D:/tools/26ai/dbhomeFree` |

### 2.2 前端依赖

| 依赖 | 版本 |
|------|------|
| Node.js | 18+ |
| npm | 9+ |

### 2.3 目录结构

```
mis/
├── backend/                    # C++17 后端
│   ├── CMakeLists.txt          # CMake 构建配置
│   ├── include/
│   │   ├── controllers/        # Controller 头文件
│   │   ├── dao/                # DAO 层头文件（OCI 参考）
│   │   ├── models/             # 数据模型头文件
│   │   ├── services/           # Service 层头文件
│   │   ├── utils/              # 工具类（Jwt, RequestContext）
│   │   └── nlohmann/           # JSON 库（已捆绑）
│   └── src/
│       ├── main.cpp            # 入口 + JWT 鉴权中间件
│       ├── controllers/        # 路由处理器
│       ├── services/           # 业务逻辑
│       ├── dao/                # OCI 数据库操作
│       ├── models/             # 模型定义
│       └── utils/              # JWT 实现
├── frontend/                   # Vue 3 前端
│   ├── src/
│   │   ├── api/                # Axios API 封装
│   │   ├── components/         # 通用组件
│   │   ├── views/              # 页面视图
│   │   ├── router/             # 路由配置
│   │   ├── store/              # Pinia 状态管理
│   │   └── assets/             # 样式资源
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
├── database/                   # 数据库脚本
│   ├── schema/tables.sql       # 建表 DDL
│   ├── procedures/             # 存储过程
│   └── triggers/               # 触发器
├── docs/API.md                 # API 接口文档
└── 使用说明.md                  # 本文档
```

---

## 3. 启动系统

### 3.1 启动后端

```bash
# 进入后端目录
cd backend

# 构建（CMake + MinGW）
mkdir build && cd build
cmake .. -G "MinGW Makefiles"
mingw32-make -j

# 启动
./mis_backend.exe
```

> 无 Oracle 环境时自动降级为内存存储，所有数据存于进程内存中，重启后丢失。

**启动日志示例：**
```
[WMS] 未编译 Oracle 支持，使用内存存储模式
[WMS] 后端已启动 → http://0.0.0.0:8080
[WMS] API 根路径 → http://localhost:8080/api/inventory
```

### 3.2 启动前端

```bash
# 进入前端目录
cd frontend

# 安装依赖（首次）
npm install

# 开发模式启动
npm run dev
```

> 前端默认代理 API 请求至 `http://localhost:8080/api`，可在 `.env` 中修改。

### 3.3 环境变量

**前端 `.env` 文件：**

```env
VITE_API_BASE_URL=/api           # API 基地址
VITE_USE_MOCK=false              # 是否启用 Mock 数据（开发时可设为 true）
```

---

## 4. 用户角色与登录

### 4.1 内置演示账号

| 用户名 | 密码 | 角色 | 角色名 | 权限范围 |
|--------|------|------|--------|----------|
| `admin` | `123456` | admin | 系统管理员 | 全部权限 |
| `keeper` | `123456` | keeper | 库管员 | 入库登记、看板 |
| `buyer` | `123456` | purchaser | 采购员 | 入库登记、供应商管理 |
| `data_mgr` | `123456` | data_manager | 数据管理员 | SKU/商品管理 |

### 4.2 角色权限矩阵

| 页面/功能 | admin | keeper | purchaser | data_manager |
|-----------|-------|--------|-----------|--------------|
| 库存看板 | ✅ | ✅ | ✅ | ✅ |
| 入库登记 | ✅ | ✅ | ✅ | ❌ |
| SKU/商品管理 | ✅ | ❌ | ❌ | ✅ |
| 供应商管理 | ✅ | ❌ | ✅ | ❌ |
| 系统设置 | ✅ | ❌ | ❌ | ❌ |

### 4.3 登录流程

1. 打开浏览器访问 `http://localhost:5173`（前端开发服务器）
2. 默认跳转至登录页（`/login`）
3. 输入用户名和密码，点击「登录」
4. 登录成功后自动跳转至库存看板
5. Token 有效期 24 小时，过期后自动跳回登录页

---

## 5. 功能模块使用指南

### 5.1 库存看板（Dashboard）

**路径：** `/dashboard`
**适用角色：** admin、keeper、purchaser、data_manager

看板展示以下关键指标：

| 指标 | 说明 |
|------|------|
| 总库存 | 当前所有库存商品的总数量 |
| 今日入库 | 当天累计入库数量 |
| 低库存 SKU | 库存量低于安全值的商品数 |
| 异常记录 | 需复核的异常操作记录数 |
| 入库趋势图 | 近 7 天每日入库量变化曲线 + 柱状图 |

操作：
- 点击「刷新」按钮更新数据
- 点击「前往入库登记」跳转至入库页面

### 5.2 入库管理（Inbound）

**路径：** `/inbound`
**适用角色：** admin、keeper、purchaser

入库管理提供两种模式：

#### 模式一：快速入库

适用于单个 SKU 的快捷操作：

1. **仓库** — 默认 `DEFAULT`，只读
2. **SKU 编号** — 输入商品数值 ID（如 `1001`，**不支持非数字编码**）
3. **数量** — 输入入库数量
4. 点击「提交入库」

#### 模式二：采购入库单

适用于完整的采购入库流程：

**第一步 — 创建入库单（DRAFT）：**
1. 选择供应商
2. 添加商品明细行（商品 ID、数量、单价）
3. 提交创建 → 自动进入草稿状态

**第二步 — 提交审核（DRAFT → SUBMITTED）：**
- 在订单列表中操作「提交」
- 草稿状态才可提交

**第三步 — 收货确认（SUBMITTED/PARTIAL → RECEIVED）：**
- 对明细行操作「收货」
- 输入本次收货数量
- 可分批收货，全部收满后自动标记为 RECEIVED

**第四步 — 取消（任意状态 → CANCELLED）：**
- 仅 DRAFT / SUBMITTED / PARTIAL 可取消
- RECEIVED 状态不可取消（需走退货流程）
- 取消后已收库存自动回退

**入库单状态流转：**
```
DRAFT → SUBMITTED → PARTIAL → RECEIVED
  ↓         ↓          ↓
  └──── CANCELLED ←────┘
```

### 5.3 SKU / 商品管理

**路径：** `/sku`
**适用角色：** admin、data_manager

- 查看商品列表（SKU ID、名称、分类、单价等）
- 新增商品
- 编辑商品信息
- 删除商品

### 5.4 供应商管理

**路径：** `/supplier`
**适用角色：** admin、purchaser

- 查看供应商列表
- 新增供应商
- 编辑供应商信息
- 删除供应商

### 5.5 系统设置

**路径：** `/settings`
**适用角色：** admin

- 用户管理：查看所有用户、修改用户角色

---

## 6. REST API 文档

### 6.1 认证接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/auth/login` | 用户登录 | 无需 |
| POST | `/api/auth/register` | 用户注册 | 无需 |
| POST | `/api/auth/logout` | 退出登录 | 无需 |
| GET | `/api/auth/me` | 获取当前用户信息 | Bearer Token |
| GET | `/api/users` | 用户列表（管理员） | Bearer Token (admin) |
| PUT | `/api/users/{id}/role` | 修改用户角色 | Bearer Token (admin) |

### 6.2 入库管理接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/inventory/inbound` | 创建入库单 | Bearer Token |
| GET | `/api/inventory/inbound` | 入库单列表 | Bearer Token |
| GET | `/api/inventory/inbound/{id}` | 入库单详情 | Bearer Token |
| POST | `/api/inventory/inbound/{id}/submit` | 提交入库单 | Bearer Token |
| POST | `/api/inventory/inbound/{id}/cancel` | 取消入库单 | Bearer Token |
| POST | `/api/inventory/inbound/lines/{lineId}/receive` | 收货确认 | Bearer Token |

### 6.3 看板接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/inventory/dashboard` | 看板数据（含趋势） | Bearer Token |

### 6.4 通用响应格式

```json
// 成功
{ "code": 0, "message": "ok", "data": { ... } }

// 失败
{ "code": -1, "message": "错误描述" }
```

### 6.5 入库错误码

| Code | 说明 |
|------|------|
| -1 | 商品编号/SKU 必须为数值型 ID |
| -100 | 入库明细行不存在 |
| -101 | 入库单已取消 |
| -102 | 入库单已全部到货 |
| -103 | 收货量超出订购量 |
| -200 | 乐观锁版本冲突 |
| -300 | 入库单不存在 |
| -301 | 入库单已取消 |
| -302 | 已到货的入库单不可取消 |
| -400 | 非草稿状态不可提交 |
| -99 | 服务器内部错误 |

---

## 7. 数据库说明

### 7.1 核心表结构

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `users` | 系统用户 | user_id, username, password_hash, role |
| `products` | 商品/SKU | product_id, product_name, category_id, unit_price |
| `categories` | 商品分类 | category_id, category_name, parent_category_id |
| `suppliers` | 供应商 | supplier_id, supplier_name, contact_name, contact_phone |
| `product_suppliers` | 商品-供应商关系 | product_id, supplier_id, supply_price |
| `inventory` | 库存（含乐观锁） | inventory_id, product_id, quantity, version |
| `inbound_orders` | 采购入库单 | inbound_id, supplier_id, status, created_by |
| `inbound_order_lines` | 入库明细行 | line_id, inbound_id, product_id, quantity_ordered |
| `audit_logs` | 审计日志 | log_id, table_name, record_id, action, old_value, new_value |

### 7.2 存储过程

| 名称 | 功能 |
|------|------|
| `proc_inbound_create` | 创建采购入库单 |
| `proc_inbound_submit` | 提交入库单（DRAFT→SUBMITTED） |
| `proc_inbound_receive` | 原子化收货确认（含乐观锁） |
| `proc_inbound_cancel` | 取消入库单（回退库存） |

### 7.3 触发器

| 名称 | 目标表 | 功能 |
|------|--------|------|
| `trg_stock_audit` | inventory | 库存变更时自动写入 audit_logs |

---

## 8. 常见问题

### Q1: 启动后端时提示 Oracle 不可用？

这是正常的。后端支持双模式：

- **Oracle 模式**：需要安装 Oracle 23ai Free 并配置 OCI
- **内存模式**：无需数据库，所有数据存储在进程内存中

无 Oracle 时自动降级为内存模式，启动日志会提示：
```
[WMS] Oracle 不可用: ...
[WMS] 降级为内存存储模式
```

> 内存模式下数据不持久化，重启后丢失。

### Q2: 前端的 Mock 模式如何开启？

修改 `frontend/.env` 文件：
```env
VITE_USE_MOCK=true
```
重启前端即可。Mock 模式下无需后端即可独立演示所有前端功能。

### Q3: 快速入库的 SKU 编号怎么填？

填写**纯数字**的商品 ID，如 `1001`、`1002`。不支持字母前缀的编码格式（如 "SKU-RF-001"）。

### Q4: 入库单创建成功后在哪里查看？

创建成功后可在入库管理页面下的「采购入库单」标签页中查看订单列表。列表展示了所有入库单及其状态。

### Q5: 收货时提示版本冲突？

这是乐观锁机制的正常工作。当多人同时操作同一商品库存时，后提交的操作会检测到版本号不匹配并拒绝。刷新页面后重试即可。

### Q6: 前端页面白屏或无法访问？

常见排查步骤：

1. 确认后端已启动（终端可见 `[WMS] 后端已启动`）
2. 确认前端开发服务器已启动（`npm run dev`）
3. 确认 `.env` 中的 `VITE_API_BASE_URL` 与后端端口一致
4. 浏览器 F12 查看网络请求是否有 401/500 错误
5. Token 过期后会自动跳转登录页，重新登录即可

---

> 本文档对应代码仓库：[GitHub - li-boss/mis](https://github.com/li-boss/mis)
